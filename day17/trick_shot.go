package day17

import (
	"bufio"
)

// x = 192..251, y = -89 .. -59

func (minTargetX int, maxTargetX int, minTargetY int, maxTargetY int) int {
	startX := 0
	startY := 0
	currX := 0
	currY := 0

	posX := 0
	posY := 0

	//iterate through y values until finding a value of x that will work?
	currX = startX
	currY = startY
	for {
		posX =+ currX
		posX =+ curr
		currX =- 1
		currY =- 1

	}
}
